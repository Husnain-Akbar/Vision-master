(function($){
     var typed= new Typed('span.txt-rotate',{
         strings:["We'are Internet Specialist","We'are Tourist" ],
         typeSpeed:100,
         backSpeed:100,
         fadeOut:false,
         smartBackspace:true,
         loop:true
     });
})(jQuery)